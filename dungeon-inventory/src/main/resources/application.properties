spring.datasource.url=jdbc:postgresql://${DB_HOST}/${DB_NAME}
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASS}
# criar a tabela "itens" no banco novo!
spring.jpa.hibernate.ddl-auto=update
